providers = ['rust']
buildImage = 'ghcr.io/railwayapp/nixpacks:latest'

[phases.setup]
nixPkgs = [
    'binutils',
    'gcc',
    '(rust-bin.stable.latest.default.override { targets = ["x86_64-unknown-linux-musl"]; })',
    'musl',
    'musl.dev',
]
onlyIncludeFiles = ['Cargo.toml', 'apps/detective', 'libs/rust']

[phases.build]
dependsOn = ['setup']
cmds = [
    'mkdir -p bin',
    'cargo build --release --package proposals-producer --target x86_64-unknown-linux-musl',
    'cp target/x86_64-unknown-linux-musl/release/proposals-producer bin',
]
onlyIncludeFiles = ['Cargo.toml', 'apps/detective', 'libs/rust']

[variables]
PROPOSALS_BIN = 'proposals-producer'

[start]
cmd = './bin/proposals-consumer'
runImage = 'debian:latest'
onlyIncludeFiles = ['./bin/proposals-consumer']
