providers = ['rust']
buildImage = 'ghcr.io/railwayapp/nixpacks:latest'

[start]
cmd = './target/release/proposals_consumer'
runImage = 'ubuntu:latest'
onlyIncludeFiles = ['./target/release/proposals_consumer']

[phases.build]
cmds = ['cargo build --release --package proposals_consumer']

[phases.setup]
nixPkgs = ['binutils', 'gcc', '(rust-bin.stable.latest.default)']
