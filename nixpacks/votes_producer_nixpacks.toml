providers = ['rust']
buildImage = 'ghcr.io/railwayapp/nixpacks:latest'

[start]
cmd = './bin/votes_producer'
runImage = 'ubuntu:jammy'
onlyIncludeFiles = ['./bin/votes_producer']

[phases.build]
cmds = [
    'mkdir -p bin',
    'cargo build --release --package votes_producer --target x86_64-unknown-linux-gnu',
    'cp target/x86_64-unknown-linux-gnu/release/votes_producer bin',
]

[phases.setup]
nixPkgs = [
    'binutils',
    'gcc',
    '(rust-bin.stable.latest.default.override { targets = ["x86_64-unknown-linux-gnu"]; })',
]
